//Discreption
//给你一个整数方阵 arr ，
//定义「非零偏移下降路径」为：
//从 arr 数组中的每一行选择一个数字，
//且按顺序选出来的数字中，相邻数字不在原数组的同一列。

#include <stdio.h>

int minFallingPathSum(int **arr, int arrSize, int *arrColSize)
{
	
	return 0;
}

int main()
{
	return 0;
}